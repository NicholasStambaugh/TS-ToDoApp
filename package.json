{
  "name": "to-do-list",
  "version": "1.0.0",
  "description": "A to-do list web app",
  "scripts": {
    "build": "sass styles.scss styles.css && tsc",
    "start": "npm run build && live-server"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "node-sass": "^4.14.1",
    "typescript": "^4.0.3"
  }
}
